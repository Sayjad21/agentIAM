"""Operational scripts.

A package rather than loose files so the tests can import what they check
(`pyproject.toml` sets `pythonpath = ["."]`), and so `mypy --strict` covers them like
everything else.
"""
