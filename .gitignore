# --- Local tooling / editor scratch (never committed) ---
# Globbed rather than named: a copy saved as `CLAUDE (2).md` slipped past the exact
# names once and reached the tree, which the §14.4 IP claim cannot afford.
CLAUDE*.md
CONTEXT*.md
.claude/
.cursor/
.aider*
.windsurfrules
*.local.md
scratch/
notes-local/

# --- Large source artifacts kept locally, not in the tree ---
*.pdf

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib60/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# --- Virtualenvs / uv ---
.venv/
venv/
ENV/
env/
.python-version

# --- Testing / coverage / profiling ---
.pytest_cache/
.hypothesis/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.benchmarks/
.mutmut-cache
mutmut-stats.json

# --- Secrets & local config ---
.env
.env.*
!.env.example
*.pem
*.key
!deploy/**/*.pub

# --- Runtime data ---
*.log
*.sqlite3
data/
volumes/
.locust/

# --- OS / editor ---
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp

# The load harness writes its server-side profile here and unlinks it in a `finally`.
# Ignored anyway because the directory is tracked docs: on Windows a terminated uvicorn can
# still hold the handle, the unlink fails, and `git add -A` would sweep a scratch file into
# the evidence folder. Observed once after a run whose py-spy step failed.
docs/benchmarks/.perf-profile-*
